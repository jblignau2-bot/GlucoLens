# Archive

Old material kept "just in case". **Nothing in here is used by the app — this
entire folder can be deleted whenever you want the disk space back.**

| Folder | What it is |
|---|---|
| `2026-04-27-full-backup/` | Complete snapshot of the project from 27 Apr 2026, including old mockups, an old APK, and `node_modules` (~200 MB). Superseded by git history. |
| `old-test-screenshots/` | Dev screenshots from early UI iterations (honeycomb passes, onboarding, coach). |
| `old-codex-logs/` | Expo dev-server logs from early testing. |

If you're looking for anything current: the app is in `../glucolens-mobile`,
the backend in `../glucolens-api`, and the docs in `../docs`.
